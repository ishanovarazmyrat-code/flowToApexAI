/**
 * Source Flow Label : Account Set Description By Industry
 * Trigger Object    : Account
 * Record Operation  : Create
 * Trigger Timing    : Before Save
 *
 * AI-generated by Einstein. Review business logic and add CRUD/FLS checks before deploying to production.
 */
public with sharing class AccountSetDescriptionByIndustryHandler implements IFlowMigrationHandler {

    public void run(
        System.TriggerOperation op,
        List<SObject> records,
        Map<Id, SObject> oldMap
    ) {
        if (op != System.TriggerOperation.BEFORE_INSERT) return;

        for (SObject sObj : records) {
            Account record = (Account) sObj;
            if (record.Industry == 'Banking') {
                record.Description = 'Financial services account';
            } else if (record.Industry == 'Technology') {
                record.Description = 'Tech sector account';
            } else if (record.Industry == 'Healthcare') {
                record.Description = 'Healthcare sector account';
            }
        }
    }
}
